These two files are your original, unmodified Base44 SDK files
(`base44Client.js` and `app-params.js`) from the uploaded dump.

They are **not imported anywhere in the active app** — the app currently
uses `src/lib/localClient.js` instead. They're kept here only so you can
plug them back in later if you decide to reconnect to a real Base44 backend
(see "Migrating back to Base44 later" in the main README).
